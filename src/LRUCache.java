public class LRUCache {
    /**
     * Use cases:
     * 1: We need to efficiently find() and return whether an entry
     *    has been inserted in the cache or not (Cache hit/miss)                  Hashmap
     * 2: We need to efficiently delete the oldest entry from this cache          Hashmap
     * 3: We need to insert an entry to the cache efficiently                     Hashmap
     * 4: We need to update an entry to the most recently position                P-queue or linkedList
                                                                                   log n       O(1)
     */




}
