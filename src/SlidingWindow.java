public class SlidingWindow {

    // Sliding window of size k, always return the max element in the window size.
    // Method1: for loop in the window O(k) which is not good
    // Method2: (DP, DFS, BFS2, Min(Max)-heap
    //          max-Heap and lazy deletion(次优解)
    // Method3:
    public int slidingWindow(int[] array) {
        return 0;
    }
}
